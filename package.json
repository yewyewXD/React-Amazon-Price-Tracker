{
  "name": "amazon-tracker",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "client-install": "cd client && npm install",
    "client-start": "cd client && npm start",
    "build": "cd client && npm run build",
    "heroku-postbuild": "npm run client-install && npm run build",
    "dev": "concurrently \"nodemon server\" \"cd frontend && npm start\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "nightmare": "^3.0.2",
    "node-fetch": "^2.6.0"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "nodemon": "^2.0.4"
  }
}
